// /lib/reducer/applyDerived.ts
import type { RootState } from "../types"

export function applyDerived(state: RootState): RootState {
  // No derived model yet — keep behaviour identical.
  return state
}