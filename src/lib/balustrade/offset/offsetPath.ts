// /lib/balustrade/offset/offsetPath.ts
import type { OffsetSpec, Vec2 } from "../../types"

// Placeholder: no behaviour change yet.
export function offsetPolyline(path: Vec2[], spec: OffsetSpec): Vec2[] {
  return path
}